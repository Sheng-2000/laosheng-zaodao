# 老盛早知道 · 项目长期记忆

## 项目信息
- 仓库：https://github.com/Sheng-2000/laosheng-zaodao.git
- 分支：main
- 部署方式：GitHub Pages（push 后自动部署）
- 本地路径：/Users/sheng/Sheng/MyData/02-任务空间/老盛的每日快讯/WorkBuddy版/

## 每日快讯生成规范（必须严格遵守）
1. 读取 template.html 了解结构和样式
2. 搜索当日新闻、AI动态、市场行情、投资观点、关注标的
3. 生成 `老盛早知道_YYYYMMDD.html` 并写入本地
4. 更新 index.html 的 reports 数组（首位插入）和 header-meta 日期
5. **【关键步骤，每次必须执行】** git add + git commit + git push origin main 部署到 GitHub Pages
   - 不能遗漏推送步骤，用户明确要求每次生成后都要推送

## 关注标的（固定清单，不可缩水）
**银行板块（7只）**：工商银行(601398)、招商银行(600036)、农业银行(601288)、宁波银行(002142)、江苏银行(600919)、杭州银行(600926)、重庆银行(601963)
**公用事业/能源/通信（6只）**：中国移动(600941)、长江电力(600900)、中国核电(601985)、大秦铁路(601006)、中国海油(600938)、中国平安(601318)

### ⚠️ 强制规则
- 生成每日快讯时，「关注标的」板块必须包含上述全部13只标的，缺一不可
- 若某只标的无重大变化，仍需保留卡片占位，内容可简化为"维持前期观点"或"暂无重大变化"
- 此清单为硬性规范，任何缩水行为视为违规

## Tab切换JS规范
switchTab 函数必须使用 classList.toggle 方式，严禁使用 inline style。

## 移动端适配规范
@media(max-width:768px) 中的 header-top 必须用 grid 语法，不能用 flex。
- **闪电图标（brand-icon）和标题（brand-title）必须在同一行**：header-top 用 `grid-template-columns: 48px 1fr`，绝不能用 `1fr`（单列）。
- header-meta 独占下一行：设置 `grid-column: 1 / -1`，同时保留 `border-left:none; padding-left:0; flex-direction:row; flex-wrap:wrap`。

## TAB 4「价值投资风向」内容规范（从2026-04-10起生效，不可缩水）

### 内容聚焦
核心聚焦：**高股息策略、估值分析、价值股池、机构价值观点**

### 六大板块顺序（固定，缺一不可）
| 序号 | 板块名称 | 卡片数量 | 说明 |
|---|---|---|---|
| 一 | 机构价值投资观点 | **3张卡片** | 中金、高盛、兴业等券商/机构研报观点 |
| 二 | 雪球·集思录价值投资社区情绪 | **4条情绪柱状图** | 必须使用sentiment-bar-wrap样式 |
| 三 | 高股息板块深度分析 | **4张卡片** | 银行、能源、公用事业、通信各1张 |
| 四 | 低估值板块 | **2张卡片** | 错杀机会与估值修复 |
| 五 | 长线资金动向 | **2张卡片** | 险资+社保价值投资实践 |
| 六 | 价值投资估值参考表 | **1张表格** | 9项核心估值指标 |

### 附：精简参考板块（末尾，固定3张）
- REITs·固收+（1张）
- AI算力（液冷/光模块）（1张）
- 白酒/消费（1张）

### ⚠️ 强制规则（不可缩水）
- **板块数量**：六大主板块 + 附板块（3张），共14张卡片 + 1张表格 + 4条情绪柱
- **若当日某板块无重大变化**：仍需保留卡片占位，内容可写"维持前期观点"或"暂无重大变化"
- **雪球情绪样式**：必须用 `.sentiment-bar-wrap` + `.sentiment-bar` + `.sentiment-fill` 柱状图样式，不接受纯文字列表
- **估值表**：必须包含9项指标，格式固定为3列表格（指标/数值/历史分位备注）
- **任何缩水行为视为违规**

---

## 自动化任务提示词（价值投资风向板块）

```
【TAB 4: 价值投资风向】生成提示词：

内容聚焦：高股息策略、估值分析、价值股池、机构价值观点

必须包含以下结构（共14张卡片 + 1张表格 + 4条情绪柱，不可缩水）：

一、机构价值投资观点（3张卡片）
- 中金/中信证券等国内券商观点
- 高盛/摩根等国际机构观点  
- 兴业/华泰等专题研报观点
- 每张卡片包含：tag标签（tag-finance/tag-energy/tag-ai）、标题、详细观点内容

二、雪球·集思录价值投资社区情绪（4条情绪柱状图）
- 必须使用HTML结构：
  <div class="sentiment-bar-wrap">
    <div class="sentiment-label"><span>标题</span><span style="color:var(--accent)">情绪 · 百分比</span></div>
    <div class="sentiment-bar"><div class="sentiment-fill" style="width:XX%"></div></div>
  </div>
- 4个主题：银行板块讨论热度、能源/油价情绪、AI算力情绪、高股息整体仓位意愿

三、高股息板块深度分析（4张卡片）
- 银行板块（tag-finance）
- 能源板块（tag-energy）
- 公用事业（tag-policy）
- 通信/中国移动（tag-ai）

四、低估值板块（2张卡片）
- 城商行PB低位估值修复
- 大秦铁路/其他低估值标的

五、长线资金动向（2张卡片）
- 险资举牌/增配动态
- 社保基金持仓方向

六、价值投资估值参考表（1张表格，9项指标）
- 表格格式：3列（指标/数值/历史分位备注）
- 必须包含：银行板块PB、上证指数PE、银行股息率、中证红利PE、中国海油桶油利润、中国移动PB、招商银行PB、中国核电估值、黄金价格

附：精简参考板块（3张卡片）
- REITs·固收+（tag-finance）
- AI算力（液冷/光模块）（tag-chip）
- 白酒/消费（tag-policy）

⚠️ 重要：
1. 以上所有板块必须全部生成，缺一不可
2. 若当日某板块无重大变化，仍需保留卡片，内容可写"维持前期观点"
3. 雪球情绪必须用sentiment-bar-wrap柱状图样式
4. 估值表必须包含9项指标
```
