# 自动化执行记录：老盛早知道 20260901

- **执行时间**：2026-09-01（T日）；数据基准 T-1 = 2026-08-31 收盘
- **产出文件**：`老盛早知道_20260901.html`（4005 行 / 212KB）
- **index.html**：reports 数组已加入 20260901 条目（reports 现 8 条），两段 `<script>` 通过 `node --check`
- **生成器**：`g_data1.py` / `g_data2.py` / `build.py`（占位符 `re.sub` 填充；`H(c,t)` 高亮助手，避免 `%` 格式串）
- **全面 QC 结果：全部通过 ✅（42 项）**
  - 数据及时性：仅含 8/31 + 9/1，无陈旧日期
  - 结构（与模板一致）：tab-panel 8 / summary-card 29 / market-block 8 / stock-card 16 / sentiment-item 9 / sub-title 28；无 Tailwind 污染；`</style>` 行 1761；文件 4005 行
  - 数据准确性：占位符 0 / `"--"` 0 / 暂无数据 0 / 涨红跌绿矛盾 0
  - 交互：switchTab 完整 + 滚动到顶部
  - 四区字数 ≥120：机构 6 篇、操作建议 6 篇、高股息 5 卡，全部达标
  - 社区话题：5 问句 + 三角色（橙/红/绿）+ 老盛观点 + 每话题 8-12 处高亮
  - 16 关注标的：代码/股价/涨跌幅 齐全
- **本轮修复点**：社区话题循环解包（7 元组）→ 改 `for i,(title,hot,v1,v2,v3,view,tag)`；估值后缀匹配（招商银行PB 提到 银行PB 前）；操作建议/高股息扩写至 ≥120 字并加高亮；社区话题补 4-6 处高亮
- **GitHub**：`git push origin main` 成功（`e43ecc0..3afd027`），仅提交新报告+index+生成器，未触碰工作树中 11 个旧报告删除
- **状态**：完成（生成→QC→修复→推送 全流程通过）

---

## 第二轮：追加全面质检 + 高亮密度修复（同日 10:00-10:30）

- **触发**：用户追加要求"全面质量检查"（第二轮，非定时触发）
- **首要发现**：首轮 `present_files` 已回注 **1452 处 `data-page-node-id`**，工作树文件与 HEAD 差 1498 行。已 regex 清除并与 HEAD SHA256 对齐（`63daa0eb…`，`git diff` = 0）。
  - ⚠️ 教训：推送后调 `present_files` 会再次污染，须在呈现后复查清理。
- **新增深度 QC 维度**（`qc_deep.py`，33 项）：标签平衡、TAB 按钮↔面板一一对应、卡片高亮密度、高亮字号、颜色色板、页眉 ticker、老盛观点前缀、四行代码标的、CSS 与模板逐字节一致、结构 class 与模板逐项一致。
  - 判定原则：**与 template.html 对照**，模板中同样存在的记为"继承项"不算问题（本次澄清 3 项误报：TAB id `panel-N`、font-size span 72 处、卡片 class 命名 `card-body`）。
- **真实缺陷**：高亮密度大面积不达标 —— 新闻/AI 卡 **0/37**（要求 ≥5），其余文本卡 **10/52**（要求 ≥2）。
- **修复方案**：新增 `hl_boost.py` 自动高亮增强模块，在 `build.py` 合并 d1+d2 之后调用。
  - 关键坑：首版挂在 `g_data2.py` 末尾，只覆盖 g_data2 的键；`宏观_*`/`要点*`/`综评_*`/`资金_*`/`债基_*` 在 g_data1，导致 42 项仍是 0。改为独立模块 + 合并后调用才解决。
  - 词表 6 层（涨跌语义红绿 / 主题黄 / 实体黄 / 机构产品黄 / 产业技术青 / 动作橙），占位符保护已有高亮，达标即 break。
- **修复结果**：新闻/AI **37/37**、其余卡 **57/57**；全页高亮 142 → **488 处**，53 卡片均值 5.7（仅 2 张数据展示卡豁免：关注标的深度解读、关键数字速查）。
- **涨红跌绿**：class 级矛盾 **0**；颜色级 7 处经人工溯源为数据文件中语义正确的手工高亮（"美元走弱"利好→红、"加息预期升温"利空→绿、"降低月供压力"利好→红等），判定为误报。
- **QC 汇总**：基础 42/42 + 深度 33/33 = **75 项全过**
- **Git**：`3afd027..bec750b`（提交报告 + build.py + hl_boost.py + 4 个 QC 脚本 + 记忆文件）
- **可复用经验**：下一期生成时，`hl_boost.apply(D)` 已固化在 build.py 中，只需在词表缺词时补 `_TEC`/`_ORG` 两层即可自动达标。

---

## 20260902 执行摘要（定时触发，09:19 生成）

- **数据基准**：T-1 = 2026-09-01 收盘（A股/港股/亚太/美股/欧股均取 9/1 收盘；上期 20260901 为晚间二轮已含 9/1 A股，本期 A股口径相同属正常，差异靠美股/欧股推进一日+隔夜叙事全换拉开）
- **产出**：`老盛早知道_20260902.html`（244982 字节 / 占位符 0 / 无预览污染 / 字节与远端 SHA256 一致）
- **隔夜主线**：美军 9/1 空袭伊朗 → WTI +5.74% 报 90.68 美元、布油破 95；金价重挫 2.48% 报 4327.29；美 10Y 升至 4.79% 创 2025-01 来新高；美联储 9 月加息概率升至约 68%（一周前不足 40%）
- **市场**：道指 52766.88 -0.79%（三连跌）、纳指 -1.03%、DAX -1.10%；A股沪指 3979.89 -0.16%，银行逆势 +1.88%、七只创新高；南向净买入 77.57 亿港元
- **QC（qc_check.py 统一入口，以 template.html 为基线）**：占位符 0 / "--" 0 / 暂无数据 0 / 涨跌矛盾 0；高亮 37/37+57/57（全页 591 处，未改字号）；四区 ≥120 字；社区话题老盛观点 ≥120 字且含 2-4 处高亮；Tab5 十六卡同名同码逐卡复核通过
- **重要修正**：理财利率口径（1年 0.95%/3年 1.25%/5年 1.30%，上期数值偏高）；新增 qc_check.py 以模板对照判定 Tailwind/font-size 污染（避免误报 4/8 处）
- **Git**：commit `aac3884` 已 push origin/main（含报告+生成器+index+qc_check+记忆）；推送后已 regex 清 data-page-node-id 并校验 size/SHA256 一致
- **状态**：完成（生成→QC→修复→推送 全流程通过）

---

## 20260902 续跑（回应"机制不应直接调旧脚本" + B方案收尾验证）

- **背景**：上一轮用户质疑"下次生成会直接调用旧脚本、应基于搜索重新生成脚本"；已澄清机制并落地 B 方案（实时取数）+ freshness_gate 门禁。
- **本轮回合动作**：
  1. 核实 `build.py` 端到端重建产物：`老盛早知道_20260902.html` 已用实时债券值重写（美30年期 5.27% 升2bp、中10年期 1.68% 降0bp），占位符0、暂无数据0、"--"0、data-page-node-id 污染0。
  2. 全量 QC：qc_check ✅ / deep_qc ✅ / cover_qc ✅（无 FAIL）；高亮591处未改字号、四区≥120字、社区5话题全问句+三角色+老盛观点≥120字、Tab5十六卡同名同码。
  3. freshness_gate 复跑：9/2 vs 9/1 抓 23 条叙事相同句——溯源绝大多数为稳定16标的投资要点与板块特征参考（非时效新闻），用户点名的"增长动能/PMI"宏观句不在复制列表（已上轮重写解决）；门禁保留为常态护栏。
  4. 准确性核验：`_bond_str` 仅当 akshare 真实返回 `_chg` 才显"升/降N bp"，缺数据显"实时抓取"，无编造。
  5. 提交 `b3d43de` + push `00d330b..b3d43de`（含重建报告 + fetch_market.py + freshness_gate.py + 改造 build/g_data1/g_data2 + 记忆文件）；污染0、字节干净。
- **机制结论（答用户）**：定时任务触发 agent 走 5 步（搜→重写 g_data1/g_data2 叙事→build→高亮→质检→推送）；`build.py` 仅作渲染器，每轮喂入当天数据；`fetch_market` 实时抓数字替换硬编码；`freshness_gate` 拦时效叙事利旧。旧文件/旧数据非真相来源。
- **遗留**：`fetch_news.py`(联网搜当日新闻/AI/机构/社区叙事) 未建；生产环境若 sina/akshare 其他源可达，16标的/HK/商品/外汇可自动变实时。

## 20260902 续跑·三（fetch_news 落地 + build 挂死根治）

- **fetch_news.py 建成并接入**：`stock_info_global_em`(真实 标题/摘要/发布时间/链接，200条) + `stock_news_em` + `stock_hot_keyword_em`。`apply(D)` 覆盖新闻卡头条为真实带日期标题，保留 agent 分析正文（维持高亮密度）。9/2 报告已注入真实头条（含"印度外长苏杰生…"）。
- **build 挂死根因修复（关键）**：`fetch_market._run` 原 `with ThreadPoolExecutor`+`fut.result(timeout)` 在底层网络挂死时，`__exit__` 的 `shutdown(wait=True)` 无限阻塞主线程 → build 卡 7-11 分钟。改为 `threading.Thread(daemon=True)`+`join(timeout)`。同步加 `get_market` 整体 90s 上限 + fetch_market/fetch_news 当日磁盘缓存(importlib 为 g_data1/g_data2 各建独立实例、进程内缓存不共享，磁盘缓存避免重复抓网)，build 降到 ~15-90s。`build.py` 的 `data-page-node-id` 清理正则强化为两段防漏清。
- **重建 + 全 QC 通过**：qc_check/deep_qc/cover_qc 全 PASS、无 FAIL；占位符0/暂无数据0/污染0。
- **提交**：`beca2e4` → push `b3d43de..beca2e4`（fetch_news.py + 改造 fetch_market/g_data2/build + 重建报告 + 记忆）。
- **机制现状（答用户"不应直接调旧脚本"）**：定时任务 agent 走 5步（搜→重写 g_data1/g_data2 叙事→build→高亮→质检→推送）；`build.py` 仅渲染器、动态日期输出；`fetch_market` 实时抓数字、`fetch_news` 实时抓头条；`freshness_gate` 拦时效叙事利旧。数字与头条每轮真·重搜，旧文件非真相来源。
- **机构/社区/生物医学**：多角色结构化叙事无法机械合成而不编造，保留 agent 每轮重写（fetch_news 仅提供取数接口）。
- **2026-09-02 下午 用户指令：社区情绪板块 新能源→半导体**：三处联动改（`规则/template.html` 占位符/emoji/渐变、`脚本/g_data1.py` 情绪字典键+描述、`脚本/hl_boost.py` _TEC 补"半导体"），重建 9/2 报告全QC通过；提交 `e684861`→push `beca2e4..e684861`。污染坑：build 输出本干净，工具预览回注 data-page-node-id，须 build+QC+commit 同 Bash 完成再 present_files。

## 20260903 执行摘要（定时触发，10:35 生成）

- **数据基准**：T-1 = 2026-09-02 收盘。全量联网重搜后重写 g_data1/g_data2，未沿用上期成品。
- **主线**：四大行市值齐破 2 万亿（工行盘中 8.29 创新高）、军工地面兵装 +6.48% 涨停潮、
  农业有色退潮、A股缩量普跌（沪指 3941.39 -0.97%，成交 1.79 万亿连三日缩量）、
  央行净回笼 5985 亿、美股止跌（戴尔 +15.81%、博通 AI 收入 +221%）、油价破 91、金价 V 形反转。
- **产物**：`老盛早知道_20260903.html`（3983 行，高亮 636 处；占位符 0 / "--" 0 / 暂无数据 0）。
- **QC**：三套共 80 项全 PASS（qc_check 34 / deep_qc 25 / cover_qc 21，FAIL 0）。
  高亮密度 新闻·AI 卡 37/37、其余卡 57/57。
- **防照抄门禁**：PASS（叙事照抄 0 条；整体相同句 2.8%，仅 T-1 共享收盘数据）。
- **本期修复**：
  1. 报告真缺陷：社区话题「连续两日多股同创新高」原染绿(跌色)与"涨"矛盾 → 改黄 `#f0b429` 警示。
  2. 三套 QC 硬编码上期数值/日期（qc_check 关键数据、deep_qc 上证/现货金/美10Y、cover_qc 数据日）
     → 全部改为**从当期数据层动态提取**。**注意 cover_qc 的数据层变量是 `_m`，deep_qc/qc_check 是 d1/d2**。
  3. freshness_gate 增加**模板静态文案豁免**（归一化只保留汉字做子串匹配）——模板句含占位符，
     填充后数字每期不同，精确串匹配失效会误报。`图表_收益计算说明` 是数据键，非模板静态，已重写。
  4. deep_qc 语义色加 EXEMPT_POS：`相对受益`/`美元指数回落`/`美元走弱` 等字面含利空词但整体利好。
- **污染清理**：20260831(3141处)、20260902(2289处) 残留 `data-page-node-id`，已 regex 清除并随本期提交。
- **Git**：commit `90911ce` → push `20d36b5..90911ce` origin/main。
- **状态**：完成（搜→重写→build→高亮→质检→推送 全流程通过）

---

## 20260904 执行摘要（定时触发，09:11 生成）

- **数据基准**：T-1 = 2026-09-03 收盘。全量联网重搜后重写 g_data1/g_data2，未沿用上期成品（防照抄门禁：整体相同句 2.7%、叙事照抄 0）。
- **主线**：银行三连涨后跳水（早盘 42 只全红、午后 23 只翻绿）；沃勒鸽派发言推动金价单日 +2.09% 破 4478 美元；成交跌破 1.8 万亿、融资余额年内最低，非农之夜临近市场谨慎；A股指数微涨个股普跌（沪指 3942.09 +0.02%、47 股跌停 vs 41 涨停）。
- **产物**：`老盛早知道_20260904.html`（275160 字节 / 3983 行；占位符 0 / "--" 0 / 暂无数据 0 / 污染 0）。
- **QC**：三套共 80 项全 PASS（qc_check ✅ / deep_qc ✅ / cover_qc 全部 PASS / freshness_gate PASS）。高亮密度 新闻·AI 37/37、其余卡 57/57，全页 786 处、未改字号。
- **本期修复（QC 脚本误报 3 处 + 1 处配色）**：
  1. cover_qc `enclosing_color` 重构为回溯「最内层未闭合颜色 span」，解决相邻高亮串色导致"利空误用红"误报（领涨/净流出等）。
  2. cover_qc 综评4维 zone 切分由 6000 字符延长，避免「风险」维度漏检。
  3. deep_qc `EXEMPT_POS` 扩「加息预期/加息的押注」（字面含利空词但整体利好）。
  4. g_data2 过热警示「连续三日多股同创新高」原染绿(跌色)与"涨"矛盾 → 改黄 `#f0b429` 警示。
- **污染清理**：index.html 72 处 + 20260903.html 2341 处 `data-page-node-id` 已 regex 清除；index 两段 `<script>` 通过 `node --check`。
- **Git**：commit `b649cd3` → push `a63f0f0..b649cd3` origin/main（含报告+生成器+QC脚本+index）。
- **状态**：完成（搜→重写→build→高亮→质检→推送 全流程通过）
