# 2026-08-28 工作日志

## 日报生成 · 老盛早知道_20260828
- 全自动生成 2026-08-28 投资资讯日报（取 8/27 收盘 T-1），严格 v2.17 铁律：template.html 空骨架全量重生成，未利旧旧数据。
- 数据核验修复 21 处 8/27 硬伤：国际黄金 4588.98 -0.07%（方向反）、WTI 83.53 +1.58%、白银 69.23 +1.64%、上海金 995.54 +0.27%、英伟达 +8.74%、特斯拉 +2.60%、DAX +0.31% 等（date-confusion / stale 值）。
- 高亮 booster 补至 787 处（53 卡全覆盖，Tab1/2≥5）。**关键修复**：booster tokenizer 吞掉 `<!--` 注释开头的 `<`，致 123 处 `!--` 变页面垃圾文本；已给 TOKEN 加注释分支原样透传，下次须用修复版。
- QC 34/34 全 PASS；index.html 插入 20260828 并裁剪至近10天；commit `4376da7` 已 push origin/main。
- 可复用：`/tmp/gen_20260828.py`、`/tmp/boost_20260828.py`（已修注释 bug）、`/tmp/qc_20260828.py`。

## 内容加厚迭代（用户反馈"不够丰富"）
- 诊断：新闻(panel-1)/AI(panel-2) 每条仅 24-25 字一行标题流；深度解读/收尾/社区已厚。
- 改写 gen 脚本 37 条新闻/AI 元组，每条扩为「事实+背景+影响」2-3 句；联网补真实细节（英伟达962亿+106%、韩国背靠背加息、宇树+460%、人形机器人1.91万台中国占97%、韩国DRAM+401%）。
- 重跑 gen→boost→qc：新闻均89字/AI均79字，高亮787→1073，QC 34/34 全过；commit `7ef8551` 已 push origin/main。

## DOCTYPE 修复（页面顶部裸显"!DOCTYPE html>"）
- booster tokenizer 只加了 `<!-- -->` 注释透传分支，漏了 `<!DOCTYPE`（`<!`开头），重跑 boost 时丢 `<`；已在 TOKEN 的 group(1) 并入 `<![^>]*>` 声明分支。
- 8/28 重跑 gen→boost 修复；正确正则 `(?<!<)!-- ` 与 `(?m)^!DOCTYPE` 复核 8/19–8/27 全部历史报告丢`<`均为0，无需改。QC 34/34 全过。

## 机构观点区加厚（用户反馈"机构价值投资观点内容太简单"）
- 根因：panel-4「一·机构价值投资观点」6张机构卡（中信/华泰/东方/国信/银河/兴业）每卡仅一句话观点(~45字)。
- 改写 gen 脚本 ORG 列表 6 条，每卡扩为「论点+8/27真实数据支撑+配置含义」2-3句；融入中证红利PE8.66倍/股息率4.5%、银行PB0.51倍、10年国债1.7%、央行净投放6065亿/Shibor隔夜1.365%、前7月电子利润+1.1倍等。
- 正文 270字→785字(每卡116-146字)；重跑 gen→boost→qc：高亮1073→1147，QC 34/34 全过，机构观点区字数845(≥120)。commit `4c8ece9` 已 push origin/main。

## 关注标的 Tab5 内容/归类修复（用户反馈"内容不对并且归类也错了"）
- **根因**：gen 脚本 `STK` 列表顺序与 template.html 的 `{{标的N_*}}` 槽位不对应——中国银行被放到 slot4（应为招商银行）、北京银行(601169)/交通银行(601328) 这两个**根本不在关注清单**的票混入了 slot6/7、兴业银行(601166)/平安银行(000001) 被整条漏掉、中国平安被错放到 slot16（应为中国银行）。
- 表现：16 张 stock-card 的 名称/代码/标签/要点 全面错位、归类分组（国有大行/股份行/城商行/公用事业/高股息）错乱。
- 修复：将 `STK` 列表重排为与模板槽位 1→16 完全一致的顺序（工行/农行/建行/招行/兴业/平安银行/宁波/江苏/杭州/重庆/长电/大秦/移动/核电/平安/中行）；补入**真实 8/27 收盘**数据——兴业银行 18.01(-1.10%)、平安银行 11.59(-1.19%)（腾讯/雪球/东财多源核验，PB/PE/股息率同步）；剔除北京银行/交通银行。
- 重跑 gen→boost→qc：DOCTYPE 完好、div/span 平衡(1141/1392)、高亮1147、stock-card=16、**0 处北京/交通银行残留**、QC **34/34 全 PASS**（注：首次从 /tmp 跑 qc 因非 git 仓致 `git show HEAD:template.html` 失败、CSS 行误报 1772；从项目目录重跑即 34/34，属 QC 运行环境问题非报告缺陷）。
- commit `749f88a` 已 push origin/main（4c8ece9..749f88a）。可复用脚本不变。

## 社区情绪条宽度 bug 修复（用户反馈"情绪条长度和比例不成对比"）
- **根因**：gen 脚本 `sent()` 返回 `宽度=str(width)`（纯数字 "85"），写入 `D["情绪_%s_宽度"]` 后填入模板 `width:{{情绪_高股息_宽度}}` → 渲染成 `width:85`，**缺 `%` 单位**，非法 CSS 被浏览器丢弃 → 进度条全塌为 0 宽，与右侧百分比文本脱节。
- 修复：gen 脚本第74行改为 `D["情绪_%s_宽度"%k]=v["宽度"]+"%"`，9 条情绪条全部输出 `width:85%` 等合法值。模板 `width:{{..._宽度}}` 结构不变。
- 重跑 gen→boost→qc：9 条均带 `%`、无遗漏；高亮1147、div/span 平衡(1141/1392)、QC **34/34 全 PASS**（项目目录重跑）。commit `ec03d85` 已 push origin/main（749f88a..ec03d85）。
- **沉淀铁律**：生成器凡写 CSS `width`/`height` 等带单位的占位符，值必须自带单位（如 "85%"），模板不可只写 `width:{{x}}` 期望调用方补 `%`，否则易复发。
