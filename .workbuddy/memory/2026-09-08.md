# 2026-09-08 工作日志

## 日报生成（automation-1786669952391 晨间轮）
- 产出 `老盛早知道_20260908.html`（313KB，T-1 = 2026-09-07 收盘口径），全量重生成，未沿用上期。
- 主线：①算力/AI 全线反攻、创业板 +3.41%；②国有大行注资利好落地反而回调，42 家银行股全绿「利好反跌」。
- 数据源：WebSearch 多轮（A股/美股/港股/亚太/欧股/商品/16 标的/机构观点/AI 产业）+ akshare `fetch_market` 交叉；美股因 9/7 美国劳动节休市，取 09-04 收盘。

## 工程改动
- `脚本/g_data1.py`、`脚本/g_data2.py` 全量重写。
- `脚本/cover_qc.py` 三处修复：
  1. `chk()` 改为累计 `_RESULTS`，结尾按真实通过项输出「✅ 全部通过（N 项）」或「❌ 未通过 N 项：…」——原写死「（FAIL 项见上方明细）」导致尾部提示句含 FAIL 被误计。
  2. `CTX_EXEMPT` 补「提前透支/落地即兑现/利好兑现」——利好出尽语境染绿属正确，非缺陷。
  3. 市场块合法日期由单一「加密货币_收盘日期」改为**动态收集所有 `*_收盘日期` 键**，支持各市场各自的 T-1（美股休市落 09-04）。
- 防照抄门禁：第一轮 39 句重复 → 两轮同义改写后 PASS。

## 质检
- `qc_check.py` / `deep_qc.py` / `cover_qc.py` 三套全部 PASS；占位符 0、`--` 0、高亮密度达标。
- `index_sync_check.py` 三方一致 PASS（index 7 条 = 界面可见 7 = 本地文件 7）。

## 归档与推送
- 备份并 `git rm` 老盛早知道_20260901.html（备份至 /tmp/laosheng_archive_20260908/），index 同步删除该条目。
- commit `4f56cbe`，已 push 至 GitHub `Sheng-2000/laosheng-zaodao@main`（远端 HEAD 与本地一致，工作区干净）。
- 交付预览用 /tmp 静态副本，避免 present_files 污染磁盘成品（未触发回注/头部破坏）。

## 全面质检（用户追加复核请求，09:40）

### 结论：数据最新 ✅ / 无照抄 ✅ / 内容更丰富 ✅

- **数据时效性**：主体 T-1 = 9/7（「9月7日」出现 62 次）；无 2024/2025 陈旧数据（唯一「2025年1月」为美债历史比较，合法）；「7月6日」为霍尔木兹袭击统计起点，合法。
- **新闻最新**：新闻卡标题带 `(09/08)` 时间戳，系今晨 fetch_news 实抓（如「人民币中间价报6.7804 下调9点」「港股开盘恒指跌0.63%」），与市场块 T-1 口径不冲突。
- **数据自洽性抽查**：中间价 9/7=6.7795（调贬8基点）与今晨 9/8=6.7804（下调9点）差值精确吻合，无打架。
- **防照抄**：叙事照抄 **0 条**；整体相同句 9.5%（全为 T-1 共享市场数据句，主因美股劳动节休市两期同取 9/4）。
- **内容丰富度**：中文正文 24123 → **27100 字（+2977）**，Tab/卡片/标的卡数持平，内容更厚而非更薄。

### 本轮查出并修复的真实问题

1. **历史污染已推送远端**：`20260907.html` 含 **2611 处** `data-page-node-id`（占 112KB），`index.html` 含 72 处，均已随前次提交推上 GitHub。已 regex 清除并校验结构完整（`<html>/<head>/<meta>/<body>` 全在），重新 QC 结构项全 PASS。
   - 注：index.html 清理后与 HEAD 一致 → 说明**远端版本本就干净，污染只落在磁盘副本**，由 present_files 回注产生。
2. **三套 QC 增加「跨期检查守卫」**：用当期数据层检查往期报告时，数值/文案类断言（综评维度标题、关键数据、指数点位、数据日）必然误报——因为期望值来自当前 g_data。守卫逻辑：数据层主数据日不在 `[报告日-1, 报告日-5]` 内即判跨期，输出醒目警告并把 FAIL 标注为「可能含误报项」。
   - 验证：20260908（当期）三套全 PASS 无警告；20260907（历史）正确触发跨期提示。

### 教训
- **文件体积变小 ≠ 内容变少**：20260907 的 406KB 里有 112KB 是污染属性，去污后实为 297KB，正文比今天还少 2977 字。判断内容量必须看中文字数，不能看字节数。
- **复核历史报告前先认准数据层**：三套 QC 的动态取数机制决定了它们只对当期报告可信（这是设计取舍，非缺陷）。

---

## 用户反馈修复：国内外新闻「标题与正文对不上」（12:45）

- **问题**：Tab1 十六张新闻卡**全部错位**。标题是 fetch_news 抓的真实头条，正文是 agent 按主题写的分析，两者不同源。
  典型：标题「一线城市住宅租金"六连涨"」→ 正文「9月7日A股深强沪弱」；标题「汇创达CCS产品车规认证」→ 正文「财政部3000亿注资」。
- **根因**：`fetch_news.apply()` 用 `D["%s%d_标签和标题"] = 真实title` 覆盖标题键，而正文键 `*_正文` 原封不动 → 必然错位。
- **修复**（`脚本/fetch_news.py`）：
  1. 新增 `TITLE_OVERRIDE` 开关（env `LAOSHENG_NEWS_TITLE=1` 可强制开启），**默认关闭**。
  2. 恢复 `g_data2.py` 中 agent 撰写的、与正文同源的准确标题（如「银行利好反跌·42家上市银行全绿」「霍尔木兹通航量创5月来最低·伊朗亮新导弹」）。
  3. 真实新闻改为**落盘**（系统临时目录 `laosheng_news_*.json`，120 条/天）供 agent 校验事实，不再注入报告。
  4. 缓存路径由 `.workbuddy`（会被 git 跟踪）改为 `tempfile.gettempdir()`。
- **验证**：Tab1 16 张 + Tab2 21 张标题与正文主题全部对应；三套 QC 全 PASS；高亮 817 处不变；中文 26880 字（仍比昨日 +2757）。
- **提交**：`c03e003` 已推送。
- **踩坑**：`dump_news` 首版用 `io.open` 但模块未 `import io` → try/except 静默吞掉，落盘失败无提示。已补 `import io as _io`。**教训：try/except 包裹的辅助逻辑要留意"静默失败"。**
