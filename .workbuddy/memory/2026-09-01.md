# 2026-09-01 工作日志

## 老盛早知道 20260901 日报生成（自动化）
- 完整流程：读规范/质检 → 联网搜数据 → 生成 → 全面QC → 修复 → 推送 GitHub。
- 产出 `老盛早知道_20260901.html`（4005行），数据基准 8/31 收盘。
- 修复点：社区话题循环解包、估值后缀匹配（招商银行PB）、操作建议+高股息扩写≥120字、社区话题加高亮。
- 全面QC 42项全过；GitHub 推送成功（main e43ecc0..3afd027）。
- index.html reports 数组已加 20260901 条目。

## 二次全面质检（同日 10:00 追加轮）
- 发现 `present_files` 回注 1452 处 `data-page-node-id`，已清除并与 HEAD SHA256 对齐。
- 深度 QC 暴露**高亮密度大面积不达标**：新闻/AI 卡 0/37（要求≥5）、其余卡 10/52（要求≥2）。
- 新增 `hl_boost.py` 自动高亮增强模块（词表 6 层 + 占位符保护 + 达标即停），接入 build.py 合并 D 之后。
  - 首版挂在 g_data2.py 末尾只覆盖一半键（g_data1 的宏观/要点/综评/资金漏掉），改为独立模块后解决。
- 结果：新闻/AI 37/37、其余卡 57/57；全页高亮 142→488，卡片均值 5.7（仅 2 张数据展示卡豁免）。
- 涨红跌绿：class 级矛盾 0；颜色级 7 处经人工复核均为数据文件里语义正确的手工高亮（如"美元走弱"利好→红）。
- QC 汇总：基础 42/42 + 深度 33/33 = **75 项全过**；推送 `3afd027..bec750b`。
- 经验：判断"模板继承 vs 本次引入"必须用 template.html 对照（font-size span 72处、tab-panel id 均为继承，是误报）。

## 临时文件清理 + 主题跟随系统（同日 10:51）
- 清理本轮 QC 临时脚本（qc_full/qc_struct/qc_content/qc_deep.py）、`__pycache__`、`/tmp` 备份与 js 残片。
- 复查发现 present_files 再次回注追踪属性，已原子化清除并与 HEAD SHA256 对齐（git diff=0）。
- 用户问"主题能否跟随系统" → 实现：三处 init 脚本改用 `matchMedia('(prefers-color-scheme: dark)')`：无手动偏好时自动跟随系统暗/亮并实时监听变化；手动切换后存 `lszd-theme` 偏好并阻断系统监听。
  - index.html(1018)、template.html(1764)、老盛早知道_20260901.html(1764) 三处统一为 `classList.toggle('light-theme', !isDark)`。
  - node --check 三段脚本均通过，污染点 0。推送 `3f2e713..bd2bb7c`。MEMORY.md 主题约定已更新。
  - 用户追加：三文件 head 加 `<meta name="color-scheme" content="light dark">`（原生控件/滚动条跟随系统主题），index.html / template.html / 当日报告同步；推送 `16f6157`。
  - **关键纠错（11:12）**：用户指出"手动切只是临时变化、系统判断优先"，与原实现的「手动优先·阻断监听」相反。已改为：init 始终按系统值设置并监听，**不读写 localStorage**；toggleTheme 仅 `classList.toggle`（临时 flip），系统一变即覆盖。三文档逻辑一致；推送 `5faf9b0`。MEMORY 主题约定已同步修正为"系统优先·手动临时"。

## 全面质检+修复（同日 11:40）
- 用户"质量全面检查"：自写临时 QC 脚本（/tmp，跑完即删），覆盖结构/填充/标签/涨跌/日期/密度/主题/JS/密码/门户一致性。
- **45 项基础检测全过**：结构与模板 53 卡一致、占位符/暂无数据/"--"=0、div/span 全平衡、日期仅 8/31 与 9/1、涨红跌绿 class 矛盾=0、主题三文档一致(matchMedia+color-scheme meta、无 localStorage 残留、data-page-node-id 污染=0)、JS 八段 node --check 通过、密码锁 btoa 混淆=12345678、reports 8 条倒序完整且星期正确。
- 发现并修复两处真实缺陷：
  1. 页眉「每日重点事件」原 22 字超规范 20 字上限，且 index 门户标题与报告页眉措辞不一致 → 统一为「六大行中报超预期·内银领涨·油价破85」(19字)：g_data1 源头改+重跑 build.py+同步 index 标题。
  2. 涨红跌绿语义误伤 6 处（区间/配比/并列被当涨跌）：配置比例 +30%/+10%、"红利+成长"、"公用事业 +"(模板硬编码绿、与同组黄不一致)、就业率 90-95%、10Y国债 1.78%-1.79%、"降低成本80%+" → 改为中性黄/数据青；g_data1/g_data2 5 处 + template.html 1 处，重跑后加/减号染色误伤归零（仅剩真实增幅语境）。
- 全部修改未提交（待用户确认推送）。历史遗留：index 历史期(0825/0826/0827 等)标题与页眉略有差异、0826/0825 页眉超 20 字；git 工作树尚有先前清理遗留的删除(qc_*.py、历史报告 0601-0823)未提交。

## 「旧内容」专项核查（同日 11:46）
- 用户要求确认报告所有数据/新闻/信息均为最新。核查方法：①全报告日期分布扫描 ②与历史7期重叠率(6-gram) ③未来态措辞(将于/即将/待公布)扫描 ④联网核实关键宏观数据发布时点。
- 结论：报告确为全量重生成（与历史各期重叠率仅 5.8%~8.5%，无拷贝旧文）；市场数据全部 2026-08-31 收盘(T-1) 正确；新闻事件均 8/28–8/31 当日/近期，无早于T-1的旧数据混入；兴业证券"统计截至8月"等属机构观点引用，非旧内容。
- **唯一真实过时项**：8月官方制造业PMI 实际已于 2026-08-31 09:30 由国家统计局公布（制造业49.4%、连续5个月收缩；非制造业50.3%重返扩张；综合50.5%），但报告两处仍写"将于9月初公布"——属生成时发布时点判断错误。
  - 修复：g_data1 宏观_内容1、g_data2 财经新闻2「PMI数据展望」改为"已于8月31日公布…"；重跑 build.py（占位符/--/暂无数据=0，4005行），残留"将于9月初"=0，全报告日期仅 8/31 与 9/1。
- **历史疑点（未动，待定）**：老盛早知道_20260831.html 多处写"PMI回升至49.8%"，与实际公布值49.4%不符，疑为当期预测值误当公布值；该期为历史已发布文件，未擅自修改，提示用户是否回溯。
- 改动文件：g_data1.py、g_data2.py、老盛早知道_20260901.html（均未提交）。
- 建议固化：报告质量检查.md 可增补「宏观数据发布时点核对」项——遇"X月数据将于Y公布"类未来态须联网核实实际发布日（尤其官方PMI在月底最后一天9:30发布）。

## 关注标的归属错位修复（同日 12:42）
- 用户反馈"关注标的 中 标的 归属乱了、少量标的错了"。
- 根因：模板 Tab5 的 16 张 `stock-card` 用 `{{标的N_名称/代码/股价/涨跌幅/要点*}}` 引用，但 `g_data2.py` 的 `标的` 列表顺序曾变动（中行从 标的16 挪到 标的4、删兴业/平安银行、加神华/邮储），模板却未同步 → 从 标的4 起整体错位：邮储(标的16)被误显示为中行并贴"外汇专业行"、中行(标的4)被塞进"股份行·零售之王"贴"零售银行"、招商/宁波/江苏…等全员位移一格。
- 修复：保留 `g_data2.py` 当前 16 只真实股票（均有 8/31 真实收盘价，零编造），重写 template.html Tab5 stock-grid——按数据列表顺序重排 标的N 引用，并修正 5 个 sub-title 分组与每只的 tag：国有大行(工建农中邮储5)/股份行(招1)/城商行(宁波江苏杭州重庆4)/公用事业(长江大秦2)/高股息(移动核电平安神华4)。
- 验证：重跑 build.py（占位符/--/暂无数据=0，3983行）；脚本比对"渲染名 vs 数据源名"——16 张卡全部一致、要点与数据源 100% 吻合；残留 兴业银行/平安银行/同业之王=0；div 平衡 1139/1139；涨跌class↔符号矛盾=0。
- MEMORY.md 已同步更新：标的列表顺序固定为 工1建2农3中4招5宁波6江苏7杭州8重庆9长江10大秦11移动12核电13平安14神华15邮储16；并新增"Tab5 关注标的易错点"提示（改列表须同步重排模板卡片）。
- 改动文件：template.html、老盛早知道_20260901.html（均未提交）。注：当前 16 只包含 神华/邮储 而非 兴业银行/平安银行——若用户本意是后者，需补两只真实 8/31 数据并重排。

## 提交与推送（同日 12:49）
- 用户"提交"：git add 指定文件（不含 .DS_Store / 未跟踪的 海报/ / .trae-html-share-packages/）→ commit `85e3912`（22 文件、+154/-42607）→ `git push origin main` 成功 `0da6ae2..85e3912`。
- 提交范围含：本期全部修复（g_data1/g_data2/template/index/报告20260901）、主题三文档同步、清理(qc_*.py + 临时报告 0601/0814-0823)、MEMORY 与当日日志。
- 剩余未提交（与本次无关，保留）：.DS_Store、未跟踪的 海报/、.trae-html-share-packages/.DS_Store、.workbuddy/.DS_Store、.workbuddy/automations/.DS_Store。

## 9/1 数据刷新（当晚 19:47 追加轮）
- 用户"根据今天最新的收盘数据更新今天的报告"：把数据基准从 T-1（8/31 收盘）切换为 T（9/1 收盘），并重搜全部新闻/资讯。
- 工作树曾被删 4 个生成器文件（build/g_data1/g_data2/hl_boost）→ 先 `git checkout HEAD --` 恢复，再全量重改 g_data1/g_data2。
- 9/1 市场要点：沪指 3979.89 -0.16%、创业板 -1.32%、科创50 -2.19%（指数跌但 3386 涨/86 涨停的"高低切换"）；银行七只创新高、中行市值破2万亿、净息差1.41%拐点、中期分红2209.89亿+7.98%；全球债市抛售（英30Y 5.904%）；两油轮霍尔木兹遇袭 WTI 87.78 +2.36%；粮食涨停潮、七部门促消费。16 只关注标的填入 9/1 真实收盘价。
- **PMI 关键纠错**：上期误用"制造业49.4%/连续5月收缩、非制造业50.3%"，经国家统计局官网核实 8 月官方 PMI=制造业49.8%(升0.6pct)、非制造业49.0%(持平)、综合49.5%(升0.2)；49.4%实为"中型企业"分项。已在 g_data1 宏观_内容1 与 g_data2 财经新闻「PMI回升至49.8%」更正。
- 机构观点换血：中信/瑞银/光大/华创/摩根大通/国信（替换 兴业/中金/大摩/旧瑞银）。
- 高亮：重建后 新闻/AI 37/37、其余 57/57 达标；首轮 3 张卡不足（重点新闻6/机器人1/AI应用2），已手动补到≥5。

## 模板 涨跌class 真实 bug 修复（同轮）
- QC 发现关注标的 16 张 `stock-card` 的涨跌幅 class 渲染成 `class="stock-change up}"`/`down}`（多一个 `}`），致 `.stock-change.up/.down` 样式匹配失败、卡片涨跌颜色全部丢失。
- 根因：template.html 16 处写成 `{{标的N_涨跌class}}}`（占位符 `}}` 后多一个 `}`）。已批量改为 `{{标的N_涨跌class}}`。
- 重跑 build.py：占位符/`--`/暂无数据=0、div 1139/1139、span 806/806、涨红跌绿矛盾=0；关注标的 14 up(红)+2 down(绿) 着色恢复正常（核电/神华为跌）。
- 全部修改未提交（待用户"提交"）。index.html 9/1 标题「六大行中报超预期·内银领涨·油价破85」已准确、无需改。

## 规则目录归置 + 定时任务提示词同步（同日 20:0x）
- 用户要求把 报告生成规范.md / 报告质量检查.md / template.html 从根目录移入 规则/，并调整配套定时任务提示词。
- 移动（git rename 保留历史）：三个文件 → 规则/；根目录两个 .md 经 `git add` 实际被识别为已跟踪→rename（此前 `ls-files` 因中文转义误判为未跟踪，已更正认知）。
- 关键依赖修复：build.py:23 TPL 改为 `os.path.join(HERE,"规则","template.html")`，否则生成器读不到模板直接崩（用户未提及的隐藏依赖，已一并修）。
- 两个 .md 内部共 10 处 template.html 引用（规范8+质量检查2）改为 规则/template.html，保证文档内 cp 命令仍可执行。
- 定时任务 automation-1786669952391（日常・大福・老盛早知道，每日00:00，当前PAUSED）提示词：文件路径引用改为 规则/报告生成规范.md、规则/报告质量检查.md，并加 note「build.py 读取 规则/template.html」。
- 验证：build.py 跑通，残留占位符/`--`/暂无数据=0、高亮 37+57 达标、行数 3983；重生成报告与 HEAD diff 为空（路径改对、产物无污染）。
- 注：上轮 9/1 更新已于本会话推送（afc830b 内容 + d738260 剥离预览追踪属性，HEAD 污染=0）。本次归置尚未提交（待用户"提交"）。

## index.html 历史快报计数修复（同日 20:22）
- 用户指出"历史快报（仅保留最近6天，不含当天）"实际列了 7 天，与页眉标注不符。
- 根因：`reports` 数组含 8 条（0901→0825），`archive = reports.slice(1)` 取了除最新外全部 = 7 条历史；且数组末条 0825 在磁盘无对应 `老盛早知道_20260825.html`（最早仅 0826），是死链(404)。
- 修复：①删除 0825 死链条目；②`reports.slice(1)` 改为 `reports.slice(1,7)` 强制只列最近6天（含当天保护：slice 不含 index0）；③同步更新"只保留最近10天"过期注释为"最新一期+最近6天历史"。
- 验证：node --check 两段脚本通过；数组现 7 条（0901+6历史），slice(1,7) 取 6 条=0826~0831 且不含当天 0901，与页眉一致。
- 待跟进（未改，需用户确认）：index.html 中 0901 标题"六大行中报超预期·内银领涨·油价破85"与当日报告页眉"银行股批量新高·中行破2万亿·油价冲88"不一致。
