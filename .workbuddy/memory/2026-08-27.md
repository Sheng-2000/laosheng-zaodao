# 2026-08-27 工作日志

## 日常自动化：老盛早知道 20260827 日报生成并推送
- 从 `git show HEAD:template.html`（空骨架 4022 行 / 779 占位符 / 8 tab / 28 sub-title / 16 stock-card / 5 社区话题）全量重生成 `老盛早知道_20260827.html`（周四，取 8/26 周三收盘 T-1）。
- 数据字典经定界对齐收割（harvest.py）从今早已生成且 QC 通过的同源 20260827 报告提取 791 占位符值 + gen.py 37 个 PATCH 补值；new_boost.py（span 栈追踪 booster）全局高亮 1164→1161 处。
- QC 29 项：仅 2 项误报（tab-panel 脚本计数 bug 实为 8、sub-title 清单滞后实为 28，均经报告 vs HEAD 模板比对确认非缺陷），其余全 PASS。
- index.html：20260827 置顶、裁至 10 天、Node 校验可 parse。
- 已 commit + push origin/main（含 20260826.html 中行 addition 一并发布）。污染版 template.html 已还原 HEAD、未提交；旧报告删除保持不处理。

## 撤回"全屏适配"宽屏布局（用户要求）
- 用户问"怎么修改了 template.html，不需要按宽屏的方式"。澄清：宽屏（`max-width:1680px`+超宽屏1900/2400px断点）是 **2026-08-26 提交 `8755e5c`「日报全屏适配」** 引入、早已进 HEAD；本会话未新增宽屏，只是把被实验污染的 template.html `git checkout` 还原回 HEAD。
- 按用户要求**撤销宽屏**：template.html 与已发布 `老盛早知道_20260827.html` 各精准回退 6 处到窄屏基线——容器 `1680→1200px`、padding `44→40px`、highlight-grid `minmax 340→320px`、market-grid `310→290px`、删除 1900/2400px 两段 `@media`。git diff 确认仅布局 CSS 改动，数据/涨跌色/高亮（1078处）未碰。
- 同步更新 MEMORY.md「页面布局」约定为窄屏基线（标注 2026-08-27 撤销）。已 commit + push。

## 关注标的归类修正（两次）
- ① 招行/中行整卡对调：生成时数据字典 `标的16`↔`标的4` 键值错位，致 中国银行(601988)错置股份行区、招商银行(600036)错置国有大行区。报告已互换两卡归位（国有大行区第4卡=中行/外汇专业行；股份行区首卡=招行/零售银行）；源头 harvested.json 整组对调 `标的16↔标的4` 防反弹。已 commit bdec25f + push。
- ② 第5区"央企高股息"误称：区内 中国平安 是股份制保险龙头、非央企（移动/核电才是国资委央企）。改名为 **"高股息 · 价值标杆"**；同步改报告+模板 sub-title、深度解读组标题/总结去"央企"、高股息文案"中国平安等央企"误称；移动/核电"央企高股息"标签保留正确。harvested.json 三处键值同步。已 commit 03a9549 + push。
- 核查结论：城商行(宁波/江苏/杭州/重庆)已在独立"城商行·区域优势"区，无需改动。关注标的现 5 区分类全部准确。
