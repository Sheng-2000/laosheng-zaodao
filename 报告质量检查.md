# 老盛早知道报告质量检查清单（优化版）

> **核心原则**：先查数据，再查结构，最后查细节。不通过即打回，不得跳过。

---

## 一、数据及时性检查（⭐⭐⭐ 最高优先级）

**检查时机**：所有其他检查之前必须首先执行
**不通过处理**：发现旧数据立即打回，重新搜索替换

### 1.1 关键日期定义

- **报告日期** = T日（如5月21日）
- **数据日期** = T-1日（如5月20日）
- **模板旧日期** = template.html中残留的日期

### 1.2 自动化检测（必运行）

```bash
# 1. 检查旧日期残留（T-3日及更早，输出应≤3处）
grep -c "5月1[2-6]日\|5/1[2-6]" 老盛早知道_YYYYMMDD.html

# 2. 验证新数据已写入（关键数据出现≥10处）
grep -c "4162\.18\|科创50.*3\.2\|中美.*300亿" 老盛早知道_YYYYMMDD.html

# 3. 检查数据一致性（同一指标在不同位置应相同）
grep -n "科创50\|恒生指数\|黄金" 老盛早知道_YYYYMMDD.html | head -20
```

### 1.3 判定标准

| 检查项   | 通过标准           | 不通过信号   |
| ----- | -------------- | ------- |
| 旧日期残留 | ≤3处（仅允许合理历史回顾） | >3处     |
| 新数据覆盖 | ≥10处关键数据       | <5处     |
| 数据一致性 | 同一指标全报告一致      | 同一指标不同值 |

### 1.4 逐Tab抽查清单

| Tab   | 抽查位置           | 检查内容        |
| ----- | -------------- | ----------- |
| Tab 0 | 4张摘要卡片数据       | 是否为T-1日数据   |
| Tab 1 | 第1张新闻卡片        | 是否为最近1-2天新闻 |
| Tab 3 | A股market-block | 指数是否为T-1日收盘 |
| Tab 5 | 工商银行stock-card | 股价是否为T-1日   |
| Tab 7 | 市场面card-body   | 描述是否为T-1日市场 |

---

## 二、结构与样式检查（⭐核心检查）

### 2.1 文件完整性（自动化检查）

```bash
# 1. 文件行数检查（目标3000±300行）
wc -l 老盛早知道_YYYYMMDD.html

# 2. CSS完整性检查（</style>应在文件末尾附近）
grep -n '</style>' 老盛早知道_YYYYMMDD.html

# 3. 结构元素数量对比（必须与模板一致）
for cls in "sub-section" "stock-card" "grid-4" "grid-2" "grid-3" "insurance-flow" "insurance-card" "rate-compare" "big-num" "topic-card" "tab-panel" "sub-title" "section-title"; do
  tpl_cnt=$(grep -c "class=\"$cls\"" template.html 2>/dev/null || echo "0")
  rpt_cnt=$(grep -c "class=\"$cls\"" 老盛早知道_YYYYMMDD.html 2>/dev/null || echo "0")
  if [ "$tpl_cnt" = "$rpt_cnt" ]; then echo "✅ $cls: $rpt_cnt"; else echo "❌ $cls: 模板=$tpl_cnt 报告=$rpt_cnt"; fi
done

# 4. CSS类名一致性（输出必须为空）
grep -oP 'class="[^"]*"' template.html | sort -u > /tmp/tpl.txt
grep -oP 'class="[^"]*"' 老盛早知道_YYYYMMDD.html | sort -u > /tmp/rpt.txt
comm -23 /tmp/rpt.txt /tmp/tpl.txt  # 报告多出的类
comm -13 /tmp/rpt.txt /tmp/tpl.txt  # 报告缺失的类

# 5. 易遗漏项检查
echo "=== 易遗漏项 ==="
echo "关注标的深度解读: $(grep -c '关注标的深度解读' 老盛早知道_YYYYMMDD.html)"
echo "债券基金: $(grep -c '债券基金' 老盛早知道_YYYYMMDD.html)"
echo "国内黄金: $(grep -c '国内黄金' 老盛早知道_YYYYMMDD.html)"
echo "中国10年期国债: $(grep -c '中国10年期国债' 老盛早知道_YYYYMMDD.html)"
echo "美国10年期国债: $(grep -c '美国10年期国债' 老盛早知道_YYYYMMDD.html)"
echo "社区情绪sentiment-item: $(grep -c 'sentiment-item' 老盛早知道_YYYYMMDD.html) (必须为9个)"

# 6. 社区情绪sentiment-item详细检查
echo "=== 社区情绪sentiment-item检查 ==="
sentiment_count=$(grep -c 'class="sentiment-item"' 老盛早知道_YYYYMMDD.html)
echo "sentiment-item数量: $sentiment_count (必须为9个)"
if [ "$sentiment_count" -eq 9 ]; then
    echo "✅ 数量正确"
    echo "9个固定顺序:"
    grep -o 'sentiment-name">[^<]*' 老盛早知道_YYYYMMDD.html | head -9 | nl
else
    echo "❌ 数量错误，应为9个"
fi

# 7. Tailwind污染检查（输出必须为空）
grep -oE 'class="[^"]*(w-|h-|p-|m-|flex|grid|block|text-|bg-|rounded)' 老盛早知道_YYYYMMDD.html | head -5
```

### 2.2 快速通过标准

| 检查项        | 通过标准                | 不通过信号          |
| ---------- | ------------------- | -------------- |
| 文件行数       | 3000±300行           | <2700或>3500    |
| CSS位置      | </style>在1300-1500行 | <1000行（CSS被截断） |
| tab-panel  | 8个                  | 数量不符           |
| sub-title  | 25-26个              | 数量不符           |
| CSS类差异     | 输出为空                | 有缺失/多出         |
| Tailwind污染 | 输出为空                | 混用Tailwind类    |

### 2.3 关键结构 checklist

- [ ] **Tab 0**: 4张summary-card + 8个highlight-item-v2 + 时间线
- [ ] **Tab 3**: 8个market-block（A股/港股/亚太/欧洲/美股/大宗商品/加密货币/汇率债券）
- [ ] **Tab 5**: 13个stock-card + 📊关注标的深度解读汇总卡片
- [ ] **Tab 6**: grid-4顶部概览 + 稳健理财工具对比 + 高股息替代策略 + 理财避坑指南 + **家庭保险配置** + **黄金投资** + **债券基金** + 社区热门话题
- [ ] **Tab 7**: 今日关键词 + 宏观面/市场面/资金面/操作提示/关键数字速查 + 📌今日操作建议

**⚠️ Tab 6 特别注意**：
- 家庭保险配置 ≠ 理财工具对比（易重复）
- 黄金投资 ≠ 股息率对比（易重复）  
- 债券基金 ≠ 理财工具对比（易重复）

---

## 三、数据准确性检查

### 3.1 涨跌颜色检查

- [ ] A股/港股/美股：**涨红跌绿**（.up红色，.down绿色）
- [ ] 加密货币/外汇：涨红跌绿（国际惯例）

### 3.2 数据标注检查

- [ ] 价格/指数数据标注日期（如"5/20收盘"）
- [ ] 无旧日期残留（引用前日数据除外）

### 3.3 数据缺失检查（⭐新增）

**问题描述**：股价、指数等关键数据可能显示为"--"或缺失。

**检查方法**：

```bash
# 检查缺失数据（--）
missing_count=$(grep -c '>--<' 老盛早知道_YYYYMMDD.html)
echo "缺失数据数量: $missing_count (应为0)"

# 检查关键数据是否已填充
echo "关键数据检查:"
echo "  建设银行: $(grep -o '建设银行.*[0-9]\+\.[0-9]\+' 老盛早知道_YYYYMMDD.html | head -1)"
echo "  招商银行: $(grep -o '招商银行.*[0-9]\+\.[0-9]\+' 老盛早知道_YYYYMMDD.html | head -1)"
echo "  农业银行: $(grep -o '农业银行.*[0-9]\+\.[0-9]\+' 老盛早知道_YYYYMMDD.html | head -1)"
echo "  大秦铁路: $(grep -o '大秦铁路.*[0-9]\+\.[0-9]\+' 老盛早知道_YYYYMMDD.html | head -1)"
echo "  宁波银行: $(grep -o '宁波银行.*[0-9]\+\.[0-9]\+' 老盛早知道_YYYYMMDD.html | head -1)"
```

**常见缺失位置**：

| 位置 | 数据项 | 说明 |
|------|--------|------|
| 页头ticker | 深证/创业板/道指 | 页头指数数据 |
| 亚太市场 | KOSPI/台湾/印度/澳洲 | 可标注为"震荡" |
| 大宗商品 | 国际黄金/白银 | 美元/盎司价格 |
| 加密货币 | 以太坊 | 美元价格 |
| 估值参考 | 上证PE/中海油桶油利润/移动PB/核电PE | 估值数据 |
| 股票卡片 | 建行/招行/农行/大秦/宁波银行 | 具体股价 |

**合格标准**：缺失数据数量 = 0

---

## 四、交互功能检查

### 4.1 switchTab函数检查

```bash
# 检查函数是否正确
grep -A5 "function switchTab" 老盛早知道_YYYYMMDD.html
```

**必须包含**：

- `p.id === 'panel-' + idx`（id匹配）
- `window.scrollTo({ top: 0, behavior: 'smooth' })`（滚动到顶部）

---

## 五、原创性检查

### 5.1 检查方法

将当天报告Tab 4/5/6/7与前一天报告逐段对比：

| 检查项            | 通过标准       |
| -------------- | ---------- |
| 卡片标题           | 基于当日事件重新拟定 |
| Tab 5 bullet文字 | 基于最新市场环境撰写 |
| Tab 6/7文字      | 基于当日数据撰写   |
| 低估值板块          | 2个标的与前一天不同 |

### 5.2 快速判定

- [ ] 无>10字连续相同
- [ ] 无同义词替换+分析逻辑完全相同
- [ ] 分析角度与前一天不同

---

## 六、常见错误与红线（重点提醒）

### 6.1 🔴 极高优先级错误

| 错误                  | 后果      | 检查方式                      |
| ------------------- | ------- | ------------------------- |
| 子代理直接创建HTML文件       | CSS完全丢失 | 文件行数<2700                 |
| 模板数据未完全替换           | 整份报告过时  | grep旧日期>3处                |
| 混用Tailwind类名        | 样式污染    | grep -oE 'class="[^"]*w-' |
| switchTab缺少scrollTo | 交互失效    | grep -c "window.scrollTo" |

### 6.2 🔴 高优先级错误

| 错误          | 后果        | 检查方式                             |
| ----------- | --------- | -------------------------------- |
| 数据不一致       | 同一指标不同值   | grep同一指标                         |
| 遗漏债券基金      | 结构不完整     | grep -c "债券基金"                   |
| 遗漏今日操作建议    | 结构不完整     | grep -c "今日操作建议"                 |
| **社区情绪不完整** | **结构不完整** | **grep -c "sentiment-item" ≠ 9** |
| **Tab 6 内容重复** | **模板残留** | **内容匹配检查** |

#### 社区情绪sentiment-item检查（⭐重要）

**固定顺序（9个，必须完整）**：

```bash
# 检查sentiment-item数量必须为9个
grep -c 'class="sentiment-item"' 老盛早知道_YYYYMMDD.html

# 检查9个固定顺序（高股息→银行→公用事业→电力→AI算力→能源油价→新能源→黄金→加密货币）
grep -o 'sentiment-name">[^<]*' 老盛早知道_YYYYMMDD.html | head -9
```

**9个固定顺序**：

1. 💰 高股息
2. 🏦 银行  
3. ⚡ 公用事业
4. 🔌 电力
5. 🤖 AI算力
6. 🛢️ 能源油价
7. 🌱 新能源
8. 🥇 黄金
9. ₿ 加密货币

**合格标准**：

- 数量 = 9个
- 顺序 = 固定顺序（如上）
- 每个包含：emoji图标 + sentiment-name + progress-bar + percent + desc

#### Tab 6 内容匹配检查（⭐新增）

**问题描述**：Tab 6 中多个子章节容易出现模板内容重复，即不同标题下显示相同内容。

**检查方法**：

```bash
# 检查家庭保险配置是否正确（不应包含"理财工具"相关内容）
grep -A20 "家庭保险配置" 老盛早知道_YYYYMMDD.html | grep -c "活期存款\|1年定存\|5年定存"
# 应为0，否则说明重复了理财工具对比内容

# 检查黄金投资是否正确（不应包含"股息率对比"）
grep -A20 "黄金投资" 老盛早知道_YYYYMMDD.html | grep -c "股息率对比\|资产配置建议"
# 应为0，否则说明重复了高股息替代策略内容

# 检查债券基金是否正确（不应包含"理财工具"进度条）
grep -A20 "债券基金" 老盛早知道_YYYYMMDD.html | grep -c "活期存款\|银行股息率"
# 应为0，否则说明重复了理财工具对比内容
```

**正确内容对应**：

| 子章节 | 正确内容 | 错误内容（模板残留） |
|--------|---------|-------------------|
| 家庭保险配置 | 医疗险/重疾险/意外险/定期寿险 + 分红险提醒 | 稳健理财工具对比进度条 |
| 黄金投资 | 黄金价格走势 + 四种投资方式 | 股息率对比 + 资产配置建议 |
| 债券基金 | 纯债/一级/二级/可转债类型介绍 | 稳健理财工具对比进度条 |

**自动化检查脚本**：

```bash
echo "=== Tab 6 内容匹配检查 ==="

# 检查家庭保险配置
insurance_wrong=$(grep -A30 "家庭保险配置" 老盛早知道_YYYYMMDD.html | grep -c "活期存款\|1年定存\|5年定存")
if [ "$insurance_wrong" -gt 0 ]; then
    echo "❌ 家庭保险配置错误：包含理财工具对比内容"
else
    echo "✅ 家庭保险配置正确"
fi

# 检查黄金投资
gold_wrong=$(grep -A30 "黄金投资" 老盛早知道_YYYYMMDD.html | grep -c "股息率对比")
if [ "$gold_wrong" -gt 0 ]; then
    echo "❌ 黄金投资错误：包含股息率对比内容"
else
    echo "✅ 黄金投资正确"
fi

# 检查债券基金
bond_wrong=$(grep -A30 "债券基金" 老盛早知道_YYYYMMDD.html | grep -c "活期存款\|银行股息率")
if [ "$bond_wrong" -gt 0 ]; then
    echo "❌ 债券基金错误：包含理财工具对比内容"
else
    echo "✅ 债券基金正确"
fi
```

### 6.3 子代理使用规范

| ❌ 错误做法        | ✅ 正确做法               |
| ------------- | -------------------- |
| 子代理直接创建HTML文件 | 只让子代理输出文本，主进程负责文件操作  |
| 子代理修改模板       | 只让子代理读取模板，主进程负责复制和修改 |
| 子代理返回完整HTML   | 子代理只返回各Tab片段，主进程负责组装 |

---

## 七、成功标准

| 指标        | 目标值             | 验证命令                          |
| --------- | --------------- | ----------------------------- |
| 数据及时性     | 旧日期≤3处，新数据≥10处  | grep -c "旧日期\|新数据"            |
| 文件行数      | 3000±300行       | wc -l                         |
| 结构元素      | 与模板一致           | for循环对比                       |
| CSS类差异    | 输出为空            | comm对比                        |
| switchTab | 包含id匹配+scrollTo | grep -A5 "function switchTab" |

---

**规范版本**: v2.2
**最后更新**: 2026年5月22日

**更新说明**:

- **v2.2**: 新增Tab 6内容匹配检查（防止模板残留重复），新增数据缺失检查（防止"--"数据），补充常见缺失位置清单
- v2.1: 新增社区情绪sentiment-item检查要求（9个固定顺序），加入易遗漏项检查清单
- v2.0: 合并重复章节（零/九合并、一/六合并、八/十合并），精简检查流程，突出自动化命令
